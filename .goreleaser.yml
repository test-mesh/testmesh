version: 2
project_name: testmesh

before:
  hooks:
    - cmd: go mod tidy
      dir: cli

builds:
  - id: testmesh
    dir: cli
    binary: testmesh
    main: .
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/test-mesh/testmesh/cli/cmd.version={{.Version}}

archives:
  - name_template: >-
      {{ .ProjectName }}_{{ .Version }}_
      {{- if eq .Os "darwin" }}macOS{{- else }}{{ .Os }}{{ end }}_
      {{- .Arch }}
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files: [LICENSE, README.md]

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  name_template: "v{{.Version}}"
  prerelease: auto
  header: |
    Install: `curl -fsSL https://testmesh.io/install.sh | sh`

homebrew_casks:
  - name: testmesh
    repository:
      owner: test-mesh
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://testmesh.io"
    description: "E2E integration testing platform — YAML flows over HTTP, DB, Kafka, gRPC and more"
